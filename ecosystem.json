{
  "apps" : [{
    "name"      : "management",
    "script"    : "/var/www/nlccoc/dist/server/index.js",
    "env": {
      "COMMON_VARIABLE": "true"
    },
    "env_production" : {
      "NODE_ENV": "production"
    }
  }],
  "deploy" : {
    "production" : {
      "key": "~/.ssh/google_compute_engine",
      "user" : "natec",
      "host" : ["35.226.119.254"],
      "ref"  : "origin/master",
      "repo" : "git@github.com:qqnc/expense-tracker.git",
      "path" : "/var/www/nlccoc",
      "pre-setup": "sudo rm -rf /var/www/nlccoc",
      "post-setup": "sudo npm install --unsafe-perm;sudo npm run build:server; sudo npm run build:web",
      "pre-deploy-local" : "echo 'This is a local executed command'",
      "post-deploy" : "npm install --unsafe-perm && pm2 startOrRestart ecosystem.json --env production",
      "env"  : {
        "NODE_ENV": "production"
      }
    }
  }
}